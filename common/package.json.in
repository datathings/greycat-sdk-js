{
  "name": "@greycat${TS_SCOPE_PREFIX}/core-common",
  "version": "${GREYCAT_VERSION}",
  "description": "Greycat Common API / Utils",
  "main": "lib/index.js",
  "typings": "typings/index.d.ts",
  "author": "DataThings S.à.r.l",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "compile": "tsc -b tsconfig.build.json",
    "watch": "tsc -b tsconfig.build.json --watch",
    "test": "tslint --project '.'",
    "clean": "rm -rf lib typings",
    "package": "yarn pack --filename greycat-core-common.tgz"
  },
  "dependencies": {
    "@greycat${TS_SCOPE_PREFIX}/types": "${GREYCAT_VERSION}"
  }
}

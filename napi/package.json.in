{
  "name": "@greycat${TS_SCOPE_PREFIX}/core-napi",
  "version": "${GREYCAT_VERSION}",
  "description": "Greycat Native N-API Binding",
  "main": "lib/index.js",
  "typings": "typings/index.d.ts",
  "author": "DataThings S.à.r.l",
  "license": "MIT",
  "bin": {
    "greycat-print-lib": "./lib/print-lib.js"
  },
  "scripts": {
    "build": "yarn clean && yarn compile",
    "compile": "tsc -b tsconfig.build.json",
    "watch": "tsc -b tsconfig.build.json --watch",
    "test": "tslint --project '.'",
    "clean": "rm -rf lib/*.js lib/*.tsbuildinfo typings",
    "package": "yarn pack --filename greycat-core-napi.tgz"
  },
  "dependencies": {
    "@greycat${TS_SCOPE_PREFIX}/core-common": "${GREYCAT_VERSION}"
  }
}

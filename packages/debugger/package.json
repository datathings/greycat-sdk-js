{
  "name": "@greycat/debugger",
  "version": "0.0.0",
  "description": "Greycat Debugger Client",
  "main": "./dist/cjs/index.js",
  "typings": "./dist/typings/index.d.ts",
  "bin": {
    "greycat-debugger": "./dist/cjs/cli.js"
  },
  "author": "DataThings S.à.r.l",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "tsc -b tsconfig.json && chmod +x dist/cjs/cli.js",
    "build": "yarn clean && yarn compile",
    "pretest": "tslint --project '.'",
    "test": "mocha lib/**/*.test.js --exit --full-trace",
    "prepackage": "./../../scripts/update-pkg.js ${VERSION:-0.0.0}",
    "package": "yarn pack --filename ${DIST_DIR:-dist}/generic/ts/greycat-debugger.tgz"
  }
}

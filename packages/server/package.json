{
  "name": "@greycat/server",
  "version": "0.0.0",
  "description": "Greycat Server",
  "main": "./dist/cjs/index.js",
  "typings": "./dist/typings/index.d.ts",
  "author": "DataThings S.à.r.l",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "tsc -b tsconfig.json",
    "build": "yarn clean && yarn compile",
    "pretest": "tslint --project '.'",
    "test": "mocha lib/**/*.test.js --exit --full-trace",
    "prepackage": "./../../scripts/update-pkg.js ${VERSION:-0.0.0}",
    "package": "yarn pack --filename ${DIST_DIR:-dist}/generic/ts/greycat-server.tgz"
  },
  "dependencies": {
    "@greycat/napi": "0.0.0",
    "debug": "4.1.1"
  },
  "devDependencies": {
    "@types/debug": "4.1.5"
  }
}

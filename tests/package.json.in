{
  "private": true,
  "name": "greycat-ts-tests",
  "version": "${GREYCAT_VERSION}",
  "description": "GreyCat Tests with both N-API & WASM",
  "author": "DataThings S.à.r.l",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "compile": "tsc -b tsconfig.build.json && webpack",
    "watch": "tsc -b tsconfig.build.json -w",
    "pretest": "tslint --project '.'",
    "test": "yarn test:napi && yarn test:wasm",
    "test:napi": "mocha -r ts-node/register src/**/*.test.ts",
    "test:wasm": "#./wasm-tests.sh",
    "clean": "rm -rf lib dist",
    "package": "echo \"greycat-ts-tests is not published: no package\""
  },
  "dependencies": {
    "@greycat${TS_SCOPE_PREFIX}/core-common": "${GREYCAT_VERSION}",
    "@greycat${TS_SCOPE_PREFIX}/core-napi": "${GREYCAT_VERSION}",
    "@greycat${TS_SCOPE_PREFIX}/core-wasm": "${GREYCAT_VERSION}"
  },
  "nyc": {
    "require": [
      "ts-node/register"
    ],
    "extension": [
      ".ts"
    ],
    "reporter": [
      "html",
      "text-summary"
    ],
    "sourceMap": true,
    "instrument": true
  }
}